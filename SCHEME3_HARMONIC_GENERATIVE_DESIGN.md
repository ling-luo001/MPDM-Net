# 方案三：强抑噪与谐波先验生成恢复

## 1. 研究命题

方案二把 MPDM-Net 重构为串行的“抑噪塔 -> 恢复塔”。方案三进一步提出一个可验证的研究假设：

> 强抑噪可以优先移除宽带噪声，但也会削弱低能量谐波和语音细节；显式、可微分的语音谐波先验能够定位较可信的周期成分，再由受约束的条件生成器分别恢复周期与非周期残差，从而提高增强质量，同时抑制自由生成造成的语音幻觉。

这里的“生成恢复”不是无条件生成完整语音，而是生成受原始观测、粗增强谱和谐波支持区域共同约束的复谱残差。模型始终保留输入语音的内容锚点。

## 2. 相关思路与取舍

- [DeepFilterNet](https://arxiv.org/abs/2110.05588)：先估计谱包络增益，再用邻帧复数深度滤波恢复周期细节。方案三借鉴其“点掩码 + 局部复滤波”的强抑噪思路。
- [PHASEN](https://ojs.aaai.org/index.php/AAAI/article/view/6489)：通过频率变换捕获谐波相关性，并让幅度、相位信息交互。方案三将隐式谐波相关性改造成可观测的软 F0 分布和谐波图。
- [Neural Source-Filter](https://arxiv.org/abs/1904.12088) 与 [DDSP](https://openreview.net/pdf?id=B1x1ma4tDr)：用谐波激励与噪声成分表达语音。方案三据此拆分谐波残差和非周期残差，但仍在压缩复谱域训练。
- [VoiceFixer](https://arxiv.org/abs/2109.13731)：分析与生成两阶段恢复。方案三保留这种大结构叙事，但不引入额外声码器。
- 相位感知扩散模型说明生成式恢复有潜力，但完整扩散会显著增加训练、推理和数据成本，也更难排除内容幻觉。因此本轮采用确定性的受约束残差生成器；扩散只作为后续升级，而不是当前实验的必要组成。

## 3. 总体数据流

```text
noisy magnitude + noisy phase
              |
              v
      Stage 1 suppression tower
              |
       center mask + phase rotation
              + neighboring-frame complex deep filters
              |
              v
       coarse complex spectrum S0
          |                 |
          |                 v
          |       differentiable harmonic analyzer
          |          soft F0 posterior q(f0|S0)
          |          voicing V and harmonic map H
          |                 |
          v                 v
 [noisy complex, S0, H, V] -> Stage 2 restoration tower
                                  |
                       harmonic complex residual Rh
                       aperiodic complex residual Ra
                                  |
                    S_hat = S0 + Rh + Ra
```

## 4. 模块设计

### 4.1 强抑噪塔

抑噪主干沿用方案二的全宽 Mamba U-Net。中心输出仍由幅度掩码和单位复数相位旋转组成，保证稳定的基础估计。

在中心输出外增加四个邻帧复数滤波抽头，时间偏移为 `[-2, -1, +1, +2]`。每个抽头预测复系数和门控值，并作用于对应的原始噪声复谱：

```text
S0(t,f) = C0(t,f) * X(t,f)
          + sum_k g_k(t,f) * C_k(t,f) * X(t+k,f)
```

旁路复系数输出层零初始化，门控偏置为 `-2`。因此初始网络严格退化为中心估计，训练后才逐步启用邻帧抵消与周期建模。

### 4.2 可微分 F0/谐波分析器

分析器使用 64 个在 60--500 Hz 间对数分布的候选 F0。每个模板包含直到 Nyquist 频率的谐波，高次谐波按 `1/sqrt(h)` 衰减，并使用 0.75 个 FFT bin 的高斯带宽。

为减小共振峰谱包络对基频判断的干扰，先对 `log(1 + magnitude)` 减去局部频率平滑包络，再与 L2 归一化模板计算相似度。经温度为 `0.1` 的 softmax 得到逐帧软 F0 后验。后验与谐波模板加权组合得到连续谐波占据图，不使用不可微的 `argmax`。

分析器本身没有可学习参数。训练时，对干净谱运行同一个分析器并停止梯度，得到软教师分布。这个教师不是绝对 F0 真值，但可为噪声粗谱提供一致、无额外标注的结构监督。

清浊音门控由抑噪塔特征预测；干净模板后验的归一化负熵作为软清浊音目标。谐波图最终乘以该门控，避免无声和非周期区域被强行谐波化。

### 4.3 受约束生成恢复塔

恢复塔使用与抑噪塔相同的基础宽度，输入为六通道：

1. 原始复谱实部；
2. 原始复谱虚部；
3. 粗复谱实部；
4. 粗复谱虚部；
5. 谐波占据图；
6. 逐帧清浊音图。

恢复端包含两个独立复残差头：

- 谐波残差 `Rh` 乘以谐波支持图；
- 非周期残差 `Ra` 乘以谐波支持图的补集。

两个残差还分别经过可学习门控，并按原始幅度与粗幅度的均值缩放。残差头零初始化，恢复门控偏置为 `-2`，所以训练初始输出满足 `S_hat = S0`。

这种设计把“生成能力”限制在待补偿残差中，不允许恢复塔脱离观测自由生成完整语音。

## 5. 联合目标

原有最终输出目标全部保留：metric、magnitude、phase、complex、time 和 STFT consistency。

新增目标如下：

- `coarse_complex = 0.10`：直接监督第一阶段粗复谱，防止抑噪塔退化为只给恢复塔传递特征。
- `pitch = 0.02`：粗谱 F0 后验与干净谱软教师之间的逐帧 KL 散度。
- `voicing = 0.01`：预测清浊音值与干净后验置信度之间的 MSE。
- `harmonic_support = 0.01`：惩罚谐波残差泄漏到非谐波区域，以及非周期残差泄漏到谐波区域。

权重刻意低于主频谱目标。谐波分析器提供结构偏置，而不是替代干净语音监督。

## 6. 稳定性设计

- 深度滤波系数、两个生成残差头均为零初始化。
- 深度滤波门、恢复门和清浊音门均使用保守负偏置。
- 所有生成残差使用 `tanh` 限幅，并按观测幅度缩放。
- F0 使用软分布而非硬类别，避免候选边界处不连续。
- 保留梯度裁剪和所有 NaN/Inf 检查。
- 抑噪上下文仍以零初始化标量单向注入恢复塔。

## 7. 必要消融

正式论文实验至少应包含：

| 实验 | 邻帧复滤波 | 谐波先验 | 双生成头 | 新增先验损失 |
|---|---:|---:|---:|---:|
| 方案二基线 | 否 | 否 | 否 | 否 |
| S3-A | 是 | 否 | 否 | 仅粗谱 |
| S3-B | 是 | 是 | 单复残差头 | 粗谱 + F0/清浊音 |
| S3-C 完整方案 | 是 | 是 | 谐波/非周期双头 | 全部 |

还应报告参数量、FLOPs、峰值显存和实时率，避免只报告语音质量而忽略结构代价。

## 8. mini 阶段判据

mini 实验首先判断结构是否值得进入完整数据训练，不用它给出最终论文结论：

- 前若干千步无 NaN、梯度爆炸或明显显存异常；
- 最终损失和粗谱损失都能下降，生成残差不长期保持严格为零；
- F0 KL 与清浊音损失有下降趋势，后验不持续坍缩为均匀分布或单一候选；
- 验证 PESQ 至少不出现相对方案二持续、明显的退化；
- 若质量提升不足，应先做 S3-A/S3-B 消融定位，而不是立刻继续扩大参数量。

## 9. 后续可升级方向

只有完整方案在全数据上证明谐波先验和残差恢复有效后，再考虑：外部高精度 F0 教师、多分辨率谐波模板、谐波相位连续性损失、因果深度滤波，或在残差空间引入少步扩散/流匹配。任何生成式升级都必须保留 noisy/coarse 锚点和内容一致性验证。
