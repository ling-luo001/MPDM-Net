# Codex Start Prompt

Read these files first:

- `AGENTS.md`
- `baseline_summary.md`
- `task_requirements.md`

I will provide paper content, method sections, paper modules, or paper code through files in:

- `paper_inputs/`

Your main output should be:

- V1 / V2 / V3 Copilot prompt files in `prompts_for_copilot/`
- one short analysis report in `paper_notes/`
- rejection note in `rejected_ideas/` only if unsuitable

Do not modify source code.

Only create or modify Markdown files and experiment record files.

Git is controlled manually by me.

Do not execute Git commands.

Do not ask Copilot Agent to execute Git branch operations.

When I tell you to process a file such as `paper_inputs/P001_input.md`, read that file and follow the workflow.